{
  "name": "verilens-monorepo",
  "version": "1.2.0",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "node --test packages/shared/test/shared.test.js && node --test apps/extension/tests/unit.test.js && node --test apps/web/test/gamification.test.js && node --test apps/web/test/theme-contrast-lint.test.js",
    "test:unit": "pnpm test",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.1.0"
  }
}
